#!/bin/bash

curl "$OPENAI_API_HOST/v1/audio/transcriptions" \
  -H "Authorization: Bearer $OPENAI_API_KEY" \
  -H "Content-Type: multipart/form-data" \
  -F file="@$1" \
  -F language="en" \
  -F response_format=text \
  -F model="whisper-1"
